.cpcache/
.nrepl-port
*.jar
target/
.clj-kondo/.cache/
node_modules/
package-lock.json
.nbb/
kekkai-node.edn
build/
platform/android/.gradle/
platform/android/build/
platform/android/app/build/
platform/desktop/target/

# a node_modules SYMLINK (pointing at a sibling checkout's install) is not
# matched by the trailing-slash form above, and got committed once
node_modules
