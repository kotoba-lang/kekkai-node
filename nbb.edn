;; Source paths only — deliberately NO :deps. An `nbb.edn` with `:deps` makes nbb
;; shell out to **bb** (babashka) to resolve them, which this workspace retired as
;; a script host (ADR-2607173000) and which is absent on CI: the first CI run
;; failed with `/bin/sh: 1: bb: not found` while every local run was green, because
;; this workstation has bb installed. Siblings go on the command line:
;;
;;   CP="src:test:../bytes/src:../noise/src:../org-ietf-dns/src:../org-ietf-turn/src"
;;   kbb --backend sci --classpath "$CP" run-tests.cljk
{:paths ["src" "test"]
 :deps {
        ;; Declared HERE and not only in deps.edn: nbb reads nbb.edn and does NOT
        ;; read deps.edn or bb.edn, and the .cljs in this repo are run as
        ;; bare `nbb <file>`. A coordinate they cannot see is not a
        ;; declaration. sha and :paths are COPIED from deps.edn.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}}}
